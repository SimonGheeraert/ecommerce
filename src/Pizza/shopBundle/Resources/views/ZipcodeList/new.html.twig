{% extends '::base.html.twig' %}

{% block body -%}
    <h1>ZipcodeList creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('zipcodelist') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
