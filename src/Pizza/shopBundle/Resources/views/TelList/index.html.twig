{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TelList list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Telsortid</th>
                <th>Telnumber</th>
                <th>Contactid</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('tellist_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.telSortId }}</td>
                <td>{{ entity.telnumber }}</td>
                <td>{{ entity.contactId }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('tellist_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('tellist_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('tellist_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
