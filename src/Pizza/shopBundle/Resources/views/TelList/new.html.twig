{% extends '::base.html.twig' %}

{% block body -%}
    <h1>TelList creation</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('tellist') }}">
            Back to the list
        </a>
    </li>
</ul>
{% endblock %}
